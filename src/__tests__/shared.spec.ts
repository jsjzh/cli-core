import cron from "@/shared/cron";
import prompt from "@/shared/prompt";
import task from "@/shared/task";

describe("cron", () => {
  it("cron", () => {});
});

describe("prompt", () => {
  it("prompt", () => {});
});

describe("task", () => {
  it("task", () => {});
});
